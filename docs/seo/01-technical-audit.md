# SEO監査 Phase 1 — 技術監査

実施日: 2026-09-04
対象: `hugo --minify` でビルドした `public/` 全体（HTML 3,884ファイル ＝ 実ページ 2,543 ＋ エイリアス 1,341）
手法: ビルド成果物の全数機械チェック ＋ 本番サイトのブラウザ実測 ＋ `content/**/*.md` のリンク解析
スコープ: 1〜6章は診断（初版）。7章に 2026-09-04 の実施記録を追記した。

---

> **【重要な訂正 2026-09-04】A-1 の数値は誤りでした。**
> 初版は posts のガイド導線を `related-guide.html`（`lg-guide-card`）だけで数え、カバー率を21%と報告しました。
> しかし posts には **もう1系統の導線** `layouts/partials/extend_post_content.html` の `article-context`
> ブロックがあり、こちらが425記事に「関連ガイド」、222記事に「エリア」の導線を出していました。
> **両系統を合わせた実際のカバー率は 512/599 = 85%** です。真の課題は「2割しかない」ではなく、
> 次の2点でした。
>
> - **導線ゼロが87記事**（大半はカテゴリ「イベント」。`article-context` が大票田カテゴリを意図的に除外しているため）
> - **ガイドへの箱が2つ並ぶ記事が114件**（2系統が独立に判定しているため）
>
> 対応内容と結果は「7. 実施記録」に記載。以下 A-1 の本文は訂正済みです。

## 0. 結論

**サイトの技術的土台は良好**。内部リンク切れ 0件、構造化データ 1,079ページに実装済み、画像 alt 欠落 0件、h1 重複 0件、hreflang の言語コード表記も正しい。8月の再設計で入れた「薄いタグページの noindex」も設計どおり動いている。

posts からガイドへの導線も85%の記事に存在する（初版はこれを21%と誤って報告した。冒頭の訂正を参照）。残る課題は、導線ゼロの87記事、箱が2つ並ぶ114記事、そして画像の未最適化である。

優先度の考え方は「一括修正で効くもの → 影響が大きいもの → 個別対応」の順。

| # | 問題 | 件数 | 修正方法 | 優先 |
|---|---|---|---|---|
| A-1 | posts のうちガイド導線がゼロの記事がある | 87/599記事 | テンプレ（一括）| **高** |
| A-1b | ガイドへの箱が2つ並ぶ記事がある | 114/599記事 | テンプレ（要design判断） | 中 |
| A-2 | 画像が未最適化（Hugo画像処理が0件） | 21枚が1MB超・最大5.4MB | テンプレ＋設定（一括） | **高** |
| A-3 | `<img>` の width/height 欠落 | 約50% | テンプレ（一括） | 中 |
| A-4 | 内部ツール2つが本番公開・robots許可 | 2URL | 静的ファイル（一括） | 中 |
| A-5 | ページ送りページの canonical が1ページ目向き | 221ページ | テンプレ1行（一括） | 低 |
| B-1 | posts のタイトルが SERP 表示幅を超過 | 161/597記事 | 記事ごと | 中 |
| B-2 | 同一イベントの重複記事 | 4組8記事 | 記事ごと | 中 |
| B-3 | posts の og:image が共通画像 | 537/597記事 | 記事ごと | 低 |
| B-4 | hreflang が不完全な組 | 11ページ | 記事ごと | 低 |
| B-5 | オーファン記事 | 1本 | 記事ごと | 低 |

---

## 1. 一括修正できるもの

### A-1. posts のうちガイド導線がゼロの記事がある【高】

**posts には独立した2系統のガイド導線がある。** これを把握せずに片方だけを数えたのが初版の誤りだった。

| 系統 | 実装 | 判定に使うもの | 出ている記事数 |
|---|---|---|---|
| ① 記事コンテキスト | `layouts/partials/extend_post_content.html` の `article-context` | タグ **＋ カテゴリ** | 関連ガイド 425／エリア 222 |
| ② 関連ガイドカード | `layouts/partials/related-guide.html` ＋ `data/guide_map.yaml` | タグのみ | 127 |

| 指標 | 実測 |
|---|---|
| いずれかの導線がある posts | 512 / 599（85%） |
| 導線がゼロの posts | 87（14%） |
| ガイドへの箱が2つ並ぶ posts | 114 |
| 本文中からガイドへリンクしている posts | 0 / 616 |

**CLAUDE.md は ② のみを記述しており、① は文書化されていない。** ① のほうが判定範囲が広く（カテゴリも見る）、駅別ガイド・図書館・公園・屋内遊び場への導線を持つ。実質的な主役は ① である。

**導線ゼロの87記事はカテゴリ「イベント」が大半**。① の判定チェーンが大票田カテゴリ（イベント）を意図的に除外しており、かつテーマタグも持たない記事が受け皿を失っていた。

**影響の見立て**: 中。85%は既に導線を持つため、初版に書いた「大」は過大評価だった。ただし87記事の取りこぼしと114記事の重複表示はどちらも実在する。

**修正方法**: ① の判定チェーン末尾に地名タグのフォールバックを足す（→ 7章で実施済み）。② 側の `guide_map.yaml` に地名を足す方法もあるが、そうすると ① と ② が同時に発火して重複表示が114件から293件に増える。実際に試して確認した。

### A-2. 画像が未最適化【高】

| 指標 | 実測 |
|---|---|
| `public/images` 合計 | 60MB / 113枚 |
| 平均 | 547KB |
| 1MB超 | 21枚 |
| 最大 | 5,490KB（`img_0373.jpeg`） |
| Hugo の画像処理実行数 | 0件（ビルドログ「Processed images: 0」） |
| フォーマット | jpg 62・png 34・jpeg 12・webp 5 |

**影響の見立て**: 中〜大。本番の記事ページを実測したところ、1ページの転送量2,057KBのうち2,042KBが単一のカバー画像だった（`/posts/wheelchair-basketball-3x3-court-open/`）。LCP要素はテキストなので LCP自体は580msと速いが、モバイル回線ではこの2MBが帯域を占有する。

**修正方法**: 一括。Hugo の画像処理（`.Resize` / `.Process "webp"`）をカバー画像テンプレートに入れ、幅1200px上限・webp出力にする。元画像はそのまま置いておける。1MB超の21枚だけ先に処理しても効果の大半が取れる。

### A-3. `<img>` の width/height 欠落【中】

posts 597ページの `<img>` 1,196個のうち、width と height の両方を持つのは597個（50%）。`loading="lazy"` はほぼ全数に付いている。

**影響の見立て**: 小〜中。本番実測の CLS は 0.077 で基準値0.1未満に収まっているが、余裕は小さい。画像読み込み前の高さが確保されていない箇所がレイアウトシフトの原因になる。

**修正方法**: 一括。画像を出力しているテンプレートに、Hugo の画像処理で得た `.Width` / `.Height` を出力する。A-2 と同じ改修で同時に片付く。

### A-4. 内部ツール2つが本番公開・robots.txt 許可【中】

| URL | 本番ステータス | robots meta | sitemap | 実体 |
|---|---|---|---|---|
| `/admin/` | 200 | なし | 非掲載 | Decap CMS 管理画面 |
| `/tools/post-tool.html` | 200 | なし | 非掲載 | 記事投稿補助ツール（55KB） |
| `/__tmp-coord-editor/facility-coord-editor.html` | 404 | なし | 非掲載 | ローカルビルド残骸のみ・本番影響なし |

`robots.txt` は `Allow: /` のみで、これらを除外していない。

**機密の混入はない**ことを確認済み。`post-tool.html` に現れる `ghp_xxxx` は利用者が自分の Personal Access Token を貼るための表示例で、実トークン形式（`ghp_` + 36文字）の文字列は0件。`/admin/` は Netlify Identity 認証で保護される Decap CMS の標準構成。

**影響の見立て**: 小。リンクもサイトマップ登録もないため通常はクロールされないが、インデックスされた場合に低品質ページとして評価対象に入る。

**修正方法**: 一括。`static/robots.txt` を用意して `Disallow: /admin/` と `Disallow: /tools/` を追加するか、両ファイルに `<meta name="robots" content="noindex, nofollow">` を入れる。`__tmp-coord-editor` は `public/` の残骸なので、ビルド前クリーンで消える。

### A-5. ページ送りページの canonical が1ページ目向き【低】

221件のページ送りページ（`/posts/page/2/` 等）が `index, follow` でありながら canonical を1ページ目に向けている。

```
/categories/お知らせ/page/2/
  robots    = index, follow
  canonical = https://urayasu-portal.com/categories/お知らせ/
```

**影響の見立て**: 小。Google は現在ページ送りに自己参照 canonical を推奨しており、2ページ目以降を1ページ目に集約すると、そこからしか辿れない記事の発見が遅れる可能性がある。ただし当サイトはサイトマップに全記事が載っているため実害は限定的。

**修正方法**: 一括。canonical を自己参照に変える（テンプレート1行）。なお、ページ送りページがサイトマップに含まれていないのは正しい設計で、変更不要。

---

## 2. 記事単位の手作業が必要なもの

### B-1. posts のタイトルが SERP 表示幅を超過【中】

ブランド接尾辞「 | 浦安ぽーたる」を除いた本体タイトル長の分布。

| 長さ | 件数 |
|---|---|
| 30字以内 | 436 |
| 31字以上 | 161 |
| 平均 | 27.9字 |
| 最長 | 69字 |

日本語検索結果の表示上限は全角30字前後なので、161記事は末尾が切れる。ただし当サイトは「【浦安市】」形式の地名プレフィックス（464記事で採用）で始まるため、切れるのは末尾の補足部分であり、主要キーワードは残る。

**影響の見立て**: 中。CTR への影響はあるが致命的ではない。GSC のクエリ別 CTR（Phase 2）と突き合わせ、表示回数が多いのに CTR が低い記事から直すのが効率的。161記事を機械的に直す価値は薄い。

ガイド側は問題が小さい。第1区切りまでの長さで見ると ja 95/101、zh-tw 63/75、en 66/76 が上限内に収まっている。**8月の棚卸しで「改題」判定した en の shuttle / hotels ハブ / budget も、主要キーワードは表示範囲に収まっており、全面改題は不要**（shuttle のみ61字で境界上）。

一方、ブランド接尾辞が2段になっているページが247件ある（例: `... | Local Guide | Urayasu Portal`）。表示上は切れるので害はないが、1段に統一する余地はある。

### B-2. 同一イベントの重複記事【中】

同じ催しを別スラッグで2回公開しているものが4組。

| タイトル | URL | 公開日 |
|---|---|---|
| こどもの広場で流しそうめん体験 | `/posts/kodomo-hiroba-nagashi-somen-2026/` | 2026-07-03 |
| 同上 | `/posts/urayasu-kodomo-hiroba-nagashi-somen-202607/` | 2026-07-13 |
| 三番瀬でカニ釣り体験 | `/posts/sanbanze-crab-fishing-2026/` | 2026-08-23 |
| 同上 | `/posts/urayasu-sanbanze-crab-fishing-202610/` | 2026-08-21 |
| 青少年館で高校生向け起業講座 | `/posts/seishonenkan-kokosei-kigyo-koza-2026/` | 2026-07-03 |
| 同上 | `/posts/urayasu-seishonenkan-kokosei-kigyo-koza/` | 2026-07-13 |
| ブライトンホテルで地中海ビュッフェ | `/posts/brighton-cache-cache-mediterranean-buffet-2026/` | 2026-06-15 |
| 同上 | `/posts/brighton-hotel-cache-cache-mediterranean-buffet/` | 2026-06-20 |

**影響の見立て**: 中。同一クエリで自サイト内の2ページが競合する（カニバリゼーション）。4組にとどまるので緊急ではないが、**投稿が別環境から行われるため既存記事との突合が効いていない**ことの表れで、記事数の増加に伴って増える性質のもの。

**修正方法**: 記事ごと。残す方を決めて、もう一方に `aliases` を設定して統合する。あわせて記事化プロンプト側に「公開前に既存スラッグと突合する」手順を足すのが根本対策。

### B-3. posts の og:image が共通画像【低】

| 種別 | 件数 |
|---|---|
| 記事固有の画像 | 60 |
| 共通デフォルト画像 | 537 |

カバー画像を持つ posts は 103/616。**影響の見立て**: 小。検索順位への直接影響はない。SNS シェア時と Discover 露出時の CTR に効く。GA4 のチャネル別データ（Phase 0）で SNS 流入の規模を見てから判断するのが妥当。

### B-4. hreflang が不完全な組【低】

5言語すべてが揃っていないページが11件。

| ページ | hreflang本数 |
|---|---|
| `/travel-guide/hotels/facilities/` と en版 | 3 |
| `/travel-guide/maihama-halloween/` と en・zh-tw版 | 4 |
| `/travel-guide/pre-trip-checklist/` の en・ko・zh-tw・zh版 | 5 |

`pre-trip-checklist` は「インバウンド専用の意図設計・ja非表示は運営者確認済み（2026-08-16）」と棚卸し表に記録があり、**仕様どおり**。残る facilities とハロウィーンガイドは翻訳が揃っていないだけ。

### B-5. オーファン記事【低】

ja ガイド101本のうち、本文からの被リンクが0本なのは3件。うち2件（`/life-guide/`・`/travel-guide/`）はセクションのハブでナビから辿れるため問題ない。

実質的なオーファンは `/travel-guide/hotels/where-to-stay/` の1本のみ。CLAUDE.md の「孤立記事を作らない」ルールに照らして導線を張る対象。

被リンク分布は健全で、8月の再設計の成果が出ている。

| 被リンク数 | 件数 |
|---|---|
| 0本 | 3 |
| 1-2本 | 20 |
| 3-5本 | 33 |
| 6本以上 | 45 |

---

## 3. 問題なしと確認できた項目

以下は全数チェックで異常が見つからなかった。今回は対応不要。

| 項目 | 結果 |
|---|---|
| 内部リンク切れ | ユニーク2,804件中0件（検出1件は `/tools/post-tool.html` 内のテンプレート変数 `${slug}` で実リンクではない） |
| 構造化データ | 1,079ページに実装。posts 609/609 が BlogPosting＋BreadcrumbList、うち290が Event。travel-guide 365/376 が Article、375が BreadcrumbList、140が FAQPage。life-guide 24/25 が Article、25が BreadcrumbList、16が FAQPage |
| 画像 alt | index対象1,494ページの `<img>` 3,413個すべてに alt あり。欠落0 |
| h1 | index対象の1,488ページが h1 ちょうど1個。0個は6ページ（404・検索・管理画面などの機能ページ） |
| hreflang の実装 | 言語コードが正しい（`zh-Hans` / `zh-Hant-TW`）。x-default は en を指す。5言語完備は427ページ |
| 薄いタグページ対策 | 記事2件以下のタグ852ページが `noindex, follow`。サイトマップからも除外済み。CLAUDE.md の設計どおり稼働 |
| サイトマップ | sitemapindex ＋ 言語別5本。1,266URL。index対象との差233件はすべてページ送りページで、除外は正しい |
| canonical | 実ページ2,543件中、欠落は3件（機能ページのみ） |
| description | index対象で欠落は3件（機能ページのみ）。posts はすべて設定済みで、サイト既定文への fallback は0件 |
| meta description の重複 | 1,051ページが共通文だが、いずれもタグ・カテゴリ・ページ送りページで、記事ページの重複ではない |
| ページ表示速度（本番実測） | トップ TTFB 439ms。記事ページ TTFB 195ms・LCP 580ms・CLS 0.077 |

---

## 4. 計測メモ（再現手順）

- **抽出**: `hugo --minify` 後、`public/**/*.html` 3,884件を gawk で全数パースし TSV 化（path / alias判定 / lang / title / titleLen / description / descLen / canonical / robots / hreflang数 / JSON-LD型 / img数 / alt欠落数 / og:image / h1数）
- **文字数**: `LC_ALL=en_US.UTF-8` 下の `length()` によるコードポイント数。全角1文字＝1
- **エイリアス判定**: `http-equiv="refresh"` の有無。1,341件を集計から除外
- **JSON-LD**: `--minify` は `application/ld+json` の中身を圧縮しないため、`"@type": "` は**コロンの後に半角スペースが入る**。ここを詰めたパターンで検索すると全件0になるので注意
- **被リンク**: `content/{travel-guide,life-guide}/**/*.md`（ja のみ）本文の `href="/..."` と `](/...)` の**両形式**を集計。ナビ・カード・list テンプレートは含まない。生活ガイドは HTML の `href=` 形式が主で、markdown 記法だけを見ると全件0になる
- **URLの突合**: ファイル名ではなく**ビルド後の実URL**で行う。`access-guide.md` の実URLは `/travel-guide/urayasu-maihama-access-guide/` のようにずれる例がある
- **リンク切れ**: index対象ページから出る内部リンクをユニーク化し、パーセントデコード後に `public/` 上の実体を確認
- **サイトマップ差分**: `public/*/sitemap.xml` の `<loc>` をデコードし、robots が noindex でない実ページ集合と `comm` で比較
- **本番実測**: ブラウザで `PerformanceObserver`（`buffered: true`）により LCP と CLS を取得。キャッシュが効くと `transferSize` が0になるため、転送量はディスク上の実サイズを正とした
- **PageSpeed Insights API はキーなし共有枠が枯渇（HTTP 429）で使えなかった**。フィールドデータは Phase 0 の GSC「ウェブに関する主な指標」エクスポートで代替する

---

## 5. 次フェーズへの引き継ぎ

Phase 2（需要との突合）で解像度が上がる項目。

- **B-1 タイトル改善の対象選定**: GSC のページ×クエリ（Phase 0 の B ファイル）で「表示回数が多く CTR が低い」posts を特定してから直す。161記事を機械的に直すより効率がよい
- **A-2 画像最適化の緊急度**: GSC「ウェブに関する主な指標」で該当URLの判定を確認。「不良」判定が出ていれば優先度を上げる
- **B-3 og:image の価値**: GA4 のチャネル別データで SNS 流入の規模を見てから判断
- **A-1 の効果測定**: guide_map 拡張の前に、現在のガイド流入をベースラインとして記録しておく（8月時点: travel-guide 171クリック / life-guide 18クリック）

---

## 6. 決めてほしいこと

**4件すべて 2026-09-04 に判断済み。決定内容と実施結果は7章に記載。** 決定は次のとおり。

| # | 決定 |
|---|---|
| 1 | **(a) 地名タグに汎用カード**を採用。ただし実装先は `guide_map.yaml` ではなくテンプレート側（重複回避のため。7章参照） |
| 2 | **1MB超の21枚だけ先に処理**（実施済み。42MB→4MB） |
| 3 | **新しい方を古い方への続報とし、古い方はそのまま残す**（実施済み） |
| 4 | **ツールは使用中のためそのまま残す**（変更なし） |

以下は初版時点の選択肢の記述。

1. **A-1 で大票田タグを解禁するか**。CLAUDE.md は「イベント」「開店・閉店」「グルメ」を意図的に除外している。ただし未対応タグの上位は「浦安市」198件・「新浦安」87件・「舞浜」78件といった地名タグで、ここに触れないとカバー率は2割から大きく動かない。案は3つ。
   - (a) 地名タグに汎用カード（生活ガイドトップ／旅行ガイドトップ）を割り当てる
   - (b) 複合条件（地名＋テーマ）を `guide_map.yaml` で表現できるよう `related-guide.html` を拡張する
   - (c) 現状維持とし、テーマ系タグの取りこぼしだけ拾う
2. **A-2 の適用範囲**。1MB超の21枚だけ先に処理するか、113枚すべてをパイプラインに載せるか。前者は当日で終わり、後者はテンプレート改修を伴う。
3. **B-2 の重複記事をどう統合するか**。4組それぞれで「残す方」を決める必要がある。新しい方に寄せるか、先に公開して被リンクが付いている方に寄せるかの方針。
4. **A-4 のツール公開をどうするか**。`/tools/post-tool.html` は運用で使っているなら残す必要がある。noindex だけ付けて公開を維持するか、リポジトリから外して手元運用にするか。

---

## 7. 実施記録（2026-09-04）

運営者の判断にもとづき、A-1(a)・A-2・B-2 を実施。A-4 は「ツールを使用しているためそのまま残す」との判断により変更なし。

### A-1 地名タグのフォールバック導線（実施）

`layouts/partials/extend_post_content.html` の関連ガイド判定チェーンの末尾に、地名タグによるフォールバックを追加した。

- 舞浜・ディズニー系10タグ → `/travel-guide/`（アイコン `ti-map-2`）
- 浦安市内の地名20タグ → `/life-guide/`（アイコン `ti-home`）
- **重複防止**: `data/guide_map.yaml` のいずれかのマッピングに一致する記事、および frontmatter に `guide:` を持つ記事では、このフォールバックを出さない（`$gmMatch` で判定）。②のカードと二重に並ばないようにするため
- 行徳・妙典・南行徳・市川市は**意図的に除外**。生活ガイドは浦安市の制度・施設に特化しており、市川市側の記事には内容が合わないため

`data/guide_map.yaml` は変更していない。地名を yaml 側に足すと ① と ② が同時発火して重複が114→293件に増えることを実測で確認したため、テンプレート側に置いた。

| 指標 | 実施前 | 実施後 |
|---|---|---|
| いずれかの導線がある posts | 512 | **537** |
| 導線ゼロの posts | 87 | **62** |
| ガイドへの箱が2つ並ぶ posts | 114 | 114（増えていない） |
| ガイド系ブロックが3つの posts | 22 | 22 |

**残る62記事はタグが1つも付いていない記事が大半**で、タグベースの判定では到達できない。カテゴリによるフォールバックを足せば到達率は約94%まで上がる見込みだが、今回は範囲外とした。

### A-2 大容量画像21枚の最適化（実施）

1MB超の21枚を ImageMagick で処理した。長辺1200px上限にリサイズし、PNG は256色に量子化、JPEG は quality 82、拡張子が `.webp` なのに中身が PNG だった3枚は実際の WebP に変換した。

| 指標 | 実施前 | 実施後 |
|---|---|---|
| 21枚の合計 | 42MB | **4MB**（-88%） |
| `static/images` 全体 | 61MB | **24MB** |
| 1MB超のファイル | 21枚 | **0枚** |
| 最大ファイル | 5,490KB | 417KB |

- 全21枚を `magick identify` で再検証し、寸法・形式が正しく破損がないことを確認
- ファイル名・拡張子は変更していないため、記事側の参照修正は不要
- 画質は原本と量子化後を並べて目視確認済み（グラフィック系・写真系の両方）。劣化は認められなかった
- 記事本文の内容は変わっていないため、`lastmod` は更新していない

**残件**: `20260729-kamio-hagiwara-duo-recital-urayasu-20261003-body.webp` と `20260729-urayasu-drocks-d-link-20260807-body.webp` の2枚も拡張子が `.webp` なのに中身は PNG。1MB未満のため今回の21枚には含めなかったが、同じ変換で軽くなる。

### B-2 重複記事の続報化（実施）

4組それぞれ、**新しい方の記事を古い方への続報とし、古い方は一切変更していない**。

| 組 | 古い記事（変更なし） | 新しい記事（続報化） |
|---|---|---|
| 流しそうめん | `/posts/kodomo-hiroba-nagashi-somen-2026/`（7/3） | `/posts/urayasu-kodomo-hiroba-nagashi-somen-202607/`（7/13） |
| カニ釣り | `/posts/urayasu-sanbanze-crab-fishing-202610/`（8/21） | `/posts/sanbanze-crab-fishing-2026/`（8/23） |
| 起業講座 | `/posts/seishonenkan-kokosei-kigyo-koza-2026/`（7/3） | `/posts/urayasu-seishonenkan-kokosei-kigyo-koza/`（7/13） |
| 地中海ビュッフェ | `/posts/brighton-cache-cache-mediterranean-buffet-2026/`（6/15） | `/posts/brighton-hotel-cache-cache-mediterranean-buffet/`（6/20） |

新しい方の4記事に加えた変更は3点。

1. タイトル末尾に「（続報）」を付けて重複を解消（posts のタイトル重複は 4件 → **0件**）
2. 本文冒頭に `{{< note >}}` を挿入し、古い記事へのリンクと「何を追記したのか」を明記
3. `lastmod: 2026-09-04` を追加（CLAUDE.md の必須作業）

各組の内容差を読んだうえで note の文面を書いた。組ごとに事情が異なる。

- **流しそうめん**: 新しい方が開催時間を訂正している（古い記事「10時30分〜11時30分」→ 新しい記事「10時・11時・15時30分の3回制」）。持ち物の記載も新しい方にしかない
- **起業講座**: 新しい方が開催時間を訂正（古い記事「14時〜16時」→ 新しい記事「13時〜16時」）。会場・申込開始日・市外在住者の参加条件も新しい方にしかない
- **カニ釣り**: 新しい方に追加情報はない。古い方のほうが充実しており、同月の三番瀬海岸親水施設一般開放にも触れている。note は古い記事側の情報へ誘導する文面にした
- **地中海ビュッフェ**: 古い方に開催期間（6/1〜8/31）と電話番号があり、新しい方にはない。note でその旨を明記した

**申し送り**: 流しそうめんと起業講座は、**古い記事に残っている開催時間が誤り**である。「古い記事はそのまま残す」との判断に従い変更していないが、訂正するかどうかは別途判断が必要。

### A-4 内部ツールの公開（変更なし）

`/admin/` と `/tools/post-tool.html` は運用で使用中との判断により、noindex の付与も含めて一切変更していない。`robots.txt` も従来どおり `Allow: /` のみ。

`/tools/post-tool.html` 内のテンプレート変数 `${slug}` が内部リンク検査で1件の「リンク切れ」として検出され続けるが、実リンクではないため無害である（監査時の検出値と同じ）。

### 検証結果

- `hugo --minify` ERROR 0件
- 内部リンク先2,534件のうちリンク切れ0件（`${slug}` の1件を除く）
- 最適化した21枚すべてが `public/` に存在し、参照切れなし
- posts のタイトル重複 0件
- ガイド系ブロックが1記事に3つを超えるケース 0件
- ローカルサーバーで続報バナー・ガイドカードの描画を目視確認
